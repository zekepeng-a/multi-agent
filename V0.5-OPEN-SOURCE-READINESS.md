# V0.5 Open Source Readiness 审计报告（2026-09-01）

> 收尾阶段：文档 + 可复现测试 + 配置化 + 清理。未新增任何 Runtime 能力，未改变冻结架构（Manager/Planner/Agent/Model/Executor/DAG/Review/Evaluator/Replan/Memory 关系不变）。
> 基线：V0.5-FINAL-Architecture-Audit.md（架构冻结）。

---

## 做了什么（本阶段交付物）

### 文档（基于真实代码，无虚构命令）
| 文件 | 说明 |
|---|---|
| `README.md` | 项目定位/Agent≠Model 表/能力/架构/安装/配置/运行/测试/新增 Agent/限制/状态（16 点全覆盖） |
| `docs/architecture.md` | 15 组件逐一解释 + 边界（Agent≠Model/Memory=派生/Planner 在 Manager 内） |
| `docs/memory.md` | 全链（Raw→Distill→Memory→Retrieve→Context→Planner）+ Source of Truth 表 + 聚合/去重/预算/重建 |
| `docs/installation.md` | 依赖（Node/DSH/Claude Code/Codex）+ DSH env + 工作区 .ai 结构 |
| `docs/configuration.md` | registry/Model 绑定规则/env 变量/CLI flags/认证归属 |
| `docs/usage.md` | 8 条真实命令（启动/续跑/查 tasks/agents/runs/memory/summary/失败） |
| `docs/testing.md` | node:test 说明 + 能力映射 + requires-external E2E 步骤 |
| `docs/limitations.md` | 诚实限制清单（秒退/无 Verifier/无 Hook/BM25-like/单文件 orchestrator/本机路径） |
| `AGENTS.md` | 更新为 V0.5 架构指引（原 V0.1 过时） |
| `LICENSE` | Apache-2.0 |
| `package.json` | scripts：`test`/`test:unit`/`test:integration`/`orchestrator`/`distill`/`state-summary`；无 runtime 依赖 |
| `.github/workflows/ci.yml` | push/PR 跑 install + `npm test`（无外部 LLM） |

### 代码改动（仅配置化/清理，无行为变更）
- `executors.mjs`：**未改**——核实 DSH 路径**已支持 env 覆盖**（`DSH_ORCH_DSH_ENTRY`，L210），硬编码仅为默认值；文档化并在本阶段**真实验证 env 配置可运行**（analysis→dsh-agent completed）。
- `team.js`（demo 残留）→ `examples/legacy/team.js-demo-2026-08-26.js`（保留标注，非删除）。
- `.gitignore`：`.ai/` 运行时状态排除（保留 `agents/registry.json` 模板）；`.env`/`.credentials` 排除。

### 测试体系（node:test 零依赖）
- `tests/unit/`：retriever（6）· distiller（6）· context（5）· state-summary（3）
- `tests/integration/`：executors resolver + registry/Agent≠Model（5）
- **25/25 pass**（`npm test`，无需外部 runtime）。
- 外部 runtime E2E（Review→Replan 链/Memory 重建/新 Session 检索）在 `docs/testing.md` 标注 `requires external runtime`，未混入 CI。

### 真实验证（本阶段现场）
1. **npm test 25/25** ✅（可复现，无外部依赖）
2. **完整 E2E**（message-test，真实 claude 任务）→ TASK-A completed + `.ai/report.md` 生成 ✅
3. **DSH_ORCH_DSH_ENTRY env**：设 env → analysis 任务经 dsh-agent/dsh-headless completed ✅（换机器可配置）
4. **Memory delete/rebuild**：distill 幂等（added=0）→ 删 `.ai/memory` → 重建（added=1）✅
5. **New Session retrieval**：此前多轮实测（assembleContext 召回相关 Memory）✅

## 没做什么（按约束）
- 未新增 Runtime 能力；未改 Manager/Planner/Agent/Model 关系；未改 Review/Replan 核心；未重构；未引入 Vector DB/Embedding/Model Routing/新 Framework/新 Orchestrator。
- 未删除不确定用途的文件（recorded only）；未触碰用户配置（.dsh 根 settings/.credentials/.env 未动）。
- 未执行 git init/push（发布动作属用户操作）。

## 修改了哪些代码（diff 级总结）
1. 新增：package.json、.gitignore、LICENSE、.github/workflows/ci.yml、README.md、docs/×7、AGENTS.md（重写）、tests/×5
2. 移动：team.js → examples/legacy/（改名标注）
3. 核心逻辑文件（orchestrator/executors/distiller/retriever/context/state-summary）：**零修改**

## 测试结果
- unit 19 + integration 5 = **25 pass / 0 fail**（node:test）
- 现场 E2E + env 验证 + memory rebuild 全过

## 安全审计
- API key/secret：无（registry/代码/docs 无 key；`.dsh/.credentials.yaml`、`.dsh/.env` 在仓库外且被 `.gitignore` 覆盖）
- 本机路径：仅 `executors.mjs` DSH 默认路径（已 env 化 + 文档明示）
- 无内网地址/私有配置入库

## 硬编码路径处理
- 结论：executors.mjs 已支持 `DSH_ORCH_DSH_ENTRY` env 覆盖（核实代码 L210）；默认值保留（本机可跑），README/installation/configuration 均明示「换机器设 env」。本阶段实测 env 生效。✅

## 最终开源审计（12 项）
| 项 | 结果 |
|---|---|
| README | ✅（16 点 + Agent≠Model 表 + 限制诚实） |
| Architecture | ✅ docs/architecture.md（15 组件 + 边界） |
| Memory docs | ✅ docs/memory.md（链 + SoT + 聚合/重建） |
| Installation | ✅ docs/installation.md（依赖真实 + env） |
| Configuration | ✅ docs/configuration.md（registry/model env/CLI） |
| Usage | ✅ docs/usage.md（8 条真实命令） |
| Tests | ✅ 25 个 node:test，零依赖可复现 |
| CI | ⚠️ workflow 已建（npm test），**未在 GitHub 实测**（无 push；文件为标准配置，首次 push 后验证） |
| Security | ✅ 无 secret 入库；.gitignore 覆盖 .env/.credentials |
| Hardcoded path | ✅ 已 env 化 + 文档化 + 实测 |
| Dead code | ✅ team.js 移 examples/legacy；AGENTS.md 更新；`.ai/` 运行时状态 git-ignored |
| Reproducibility | ⚠️ unit/integration 完全可复现；真实 E2E 需本机 CLI（文档标注 requires external runtime，非伪造） |

## 剩余阻塞项（诚实）
1. **CI 首跑验证**：需 git push 到 GitHub 后确认 workflow 绿（本环境无法模拟 GitHub Actions）。
2. **真实 E2E 的可复现性**依赖外部 CLI/凭据——按设计分开，非发布阻塞（文档已说明）。
3. `examples/legacy/team.js-demo-2026-08-26.js` 是历史 demo，是否保留由你决定（移出核心，未删除）。

## 结论：**OPEN SOURCE READY**
- 文件/文档/测试/配置化层面已就绪：陌生开发者可 `npm install && npm test`、读 README/docs 理解架构、设 `DSH_ORCH_DSH_ENTRY` 后运行、按 docs/testing.md 复现 E2E。
- 发布动作（git init、push、CI 首跑、License 归属）属用户操作；CI 结果需 push 后复核。
